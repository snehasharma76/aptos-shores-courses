module robinson::my_shore {

    struct GlobalData has key {
        nb_tree: u8,
        has_river: bool,
        shore_location: address,
        daily_visitors: vector<u64>,
    }

 // Define a "struct" named House with parameters "no_of_members" of type "integer", "house_location" and "house_owner" of type "address"
 // Also check for which ability of struct should be used here

}