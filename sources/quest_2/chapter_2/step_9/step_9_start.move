module robinson::my_shore {

    struct GlobalData has key {
        nb_tree: u8,
        has_river: bool,
        shore_location: address,
        nb_trees: vector<u8>,
    }

 // Define a "struct" named House with parameters "no_of_members" of type "integer", "house_loacation" and "house_owner" of type "address"

}