//create a shoreNFT package under module `robinson`
// Import the `account` module from `aptos_framework` and bring the `SignerCapability` struct into scope
// Import the `event` module from `aptos_framework`
// Import the `object` module from `aptos_framework`
// Import the `to_string` function from the `string_utils` module in `aptos_std`
// Import the `collection` module from `aptos_token_objects`
// Import the `token` module from `aptos_token_objects`
// Import the `option` module from standard library
// Import the `address_of` function from the `signer` module in `std`
// Import the `signer` module from standard library
// Import the `string` module from `std` and bring the `String` and `utf8` types/functions into scope