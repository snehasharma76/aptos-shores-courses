//create a shoreCurrency package under module `robinson`
//import `object` library from `aptos_framework`
//import `primary_fungible_store` from `aptos_framework`
//import `signer` from standard library

//declare a function `init_module` with parameter `admin` as a signer
//declare a entry function `mint` of public visibility specifier with parameter `admin` as signer, `to` as address type and `amount` of type u64
//declare a entry function `transfer` of public visibility specifier with parameter `admin` as signer, `from` as address type, `to` as address type and `amount` of type u64
//declare a entry function `burn` of public visibility specifier with parameter `admin` as signer, `from` as address type and `amount` of type u64

